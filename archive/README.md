# Archive

Esta carpeta contiene archivos obsoletos y scripts descartados que se mantienen por referencia histórica.

**No ejecutar estos scripts.** Usar en su lugar los scripts en `scripts/`.

## Contenido

- `deprecated_scripts/`: Scripts antiguos de scraping y procesamiento
- Archivos CSV y datos antiguos que ya no se usan en el flujo actual
